<Window x:Class="VlcFolderQueue.TagsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Tags" Height="160" Width="400" WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="False"
        Background="#1E1E1E" Foreground="#E0E0E0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="30" Background="#252526" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Edit Tags" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12"/>
            <Button Grid.Column="1" Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" Width="46" Height="30"
                    Style="{StaticResource TitleBarCloseButtonStyle}" Click="CloseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="12">
            <TextBlock Text="Comma-separated tags:" Margin="0,0,0,8"/>
            <TextBox x:Name="TagsBox" Height="24"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Padding="10,4" IsCancel="True"/>
                <Button Content="OK" Padding="10,4" Margin="8,0,0,0" IsDefault="True" Click="OkButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
